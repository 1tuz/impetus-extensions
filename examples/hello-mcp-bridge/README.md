# hello-mcp-bridge

Minimal `mcp_bridge` example (stub MCP config; no binary required for manifest validation beyond `mcp.json` command string).

For a real Rust MCP server see `extensions/git-tools` or `extensions/http-fetch`.
