# hello-instruction-pack

Minimal `instruction_pack` example. Points at the canonical tutorial:

**[`extensions/hello-extension/`](../../extensions/hello-extension/)**

```bash
cargo run -p impetus-ext-support -- validate-manifests --root .
```

See [creating-extension.md](../../docs/creating-extension.md).
